---
- name: Enable Docker on startup.
  service:
    name: docker
    enabled: true

- name: "Add user '{{ admin }}' to Docker group."
  user:
    name: "{{ admin }}"
    groups: docker
    append: true
